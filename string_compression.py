def string_compression(chars):
    i = 0
    current = 1
    for j in range(0, len(chars)):
        if chars[j] :
            i += 1
            count += count
            chars[i] = count
            count = 0


    print(chars)

string_compression(["a","a","b","b","c","c","c"])
